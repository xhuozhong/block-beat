# 方块节拍 · 开发记录

## 初次本地交付的目标与范围

新建独立浏览器游戏，原 Skill 仓库不改动。原生 HTML/CSS/Canvas/JavaScript，无网络资源或运行时依赖，可双击 index.html 离线运行。单人无账号，不部署公网。

## 专家分工

- 总导演：game-studio-director，定义可玩切片与验收。
- 玩法主责：game-developer；支持 game-ui-design、gameplay-balance-validator、develop-web-game。
- 音频主责：game-audio-director，用户操作解锁、事件对应、静音与暂停。
- 玩法设计检查：game-design-theory，保持经典消行循环，不增加额外经济系统。
- 以上为按需采用的 Skill 工作流，不是自动启动多代理。

## 功能契约

玩家旋转和放置七类方块，填满整行以消除。10×20 棋盘、七袋随机、3 块预览、一次暂存/每块、落点预览、软降/硬降、450ms 落地延迟（最多 15 次动作重置）、每 10 行升级。单/双/三/四行基础分 100/300/500/800，乘消行前等级；软降每格 1 分，硬降每格 2 分；不实现 T-spin 或完整 SRS，采用简化踢墙。

游戏状态：准备、运行、暂停、结束。切走页面自动暂停，返回后手动继续。暂停/结束不计时计分。最高分和静音偏好保存本机；损坏/禁止存储不阻止游戏。触屏按钮与键盘均可操作。

## 验收

- 规则测试：随机袋、旋转边界、消行得分且不重复、等级、暂存限制、顶出、暂停、落地延迟与种子复现。
- 浏览器：开始→移动/旋转→硬降→暂存→暂停/继续→重开；专门标明的 QA 预置棋盘用于消行/升级/结束验证。
- 查看桌面与窄屏画面、界面状态、控制台与资源加载，记录实际运行结果。
- 音效只用程序合成，不用外部采样；播放状态验证不等于已试听。

## 初次本地交付状态

已实现并完成本地验收。成品为 `index.html`，本地预览地址为 http://127.0.0.1:8766 。未修改 Skill 仓库、未部署公网。

## 实际验收结果（2026-08-31）

- 使用 Node 24.19.0 运行 `node --test tests/engine.test.cjs`：22 项规则测试通过。覆盖 100 个随机袋、种子复现、四种消行、得分账目、等级、软降、暂存、旋转边界、暂停、落地延迟上限、顶出和重开。
- `node --check game.js` 语法检查通过。
- 在 Codex 内置 Chromium 浏览器实际执行开始、移动、旋转、软降、硬降、暂存、暂停和继续；重复暂存只接受一次。自然出块连续放置 7 块，实际截图已保存。
- 暂停前后跨工具调用读取状态，时间与分数不变。
- 明确标注的预置场景：单行得分 138（38 硬降 + 100 消行）；四行 834（34 硬降 + 800 消行）；9 行跨到 10 行后等级 2，消行仍按升级前等级计分；顶出进入结束状态。
- 预置场景不会写入最高分。正常测试获得的最高分 39 与静音状态在刷新后保留。交付前通过 QA 清理按钮移除本次生成的测试纪录，最高分重置为 0、音效恢复开启。
- 帮助对话框打开时暂停，关闭后仍待玩家明确继续。
- 390×844 视口实际点击手机按钮，暂存后移动/硬降仍正常；320×740 无横向溢出（内容宽度 305px，含滚动条的视口 320px）；1280×800 桌面内容无横向溢出，触屏按钮隐藏。查看过相应截图。视口测试不是实体手机测试。
- 音频通过交互解锁，AudioContext 状态为 running，旋转、暂存、锁定、消行等音效计数增长；暂停后活动音源为 0。未进行实际听检。
- 最终浏览器 error / warn 日志为空。启动按钮文字对比度、音效结束事件重复触发、音效开关后键盘焦点已在实现过程中修正。

## 明确未验证的范围

- file:// 直接启动被浏览器工具的 URL 安全策略阻止；未尝试绕过。代码使用相对路径和传统脚本，不需服务器 API；实际浏览器验收在既有 HTTP 本地服务上完成。
- 自动化中新建后台标签没有改变游戏文档焦点，不能据此证明真实标签切换已触发暂停。已实现 blur / visibilitychange 处理，暂停规则和帮助触发均经过验证；真实系统切换仍待手动检查。
- 未验证所有浏览器/实体触屏、屏幕阅读器完整游玩、实际扬声器听感、长时压力和主观难度平衡。未实现联机、存档续局、官方完整旋转规则。

## 交付与后续

- 规则源码、界面、测试、MIT 许可、说明均包含在目录内。操作与启动方式见 README。
- 浏览器状态证据在 `tests/browser-evidence.json`。截图另附于首次本地交付目录，未作为仓库源码依赖。
- 下一项可选改进：在用户确认后增加完整 SRS 旋转与 T-spin 计分；本次不额外扩展玩法。
